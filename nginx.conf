server {
    server_name data-line.ir;
    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/data-line.ir/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/data-line.ir/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

server {
    if ($host = data-line.ir) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    server_name data-line.ir;
    listen 80;
    return 404; # managed by Certbot
}

server {
    server_name data-lines.ir;  # Replace with your domain

    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/data-lines.ir/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/data-lines.ir/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}

server {
    if ($host = data-lines.ir) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name data-lines.ir;
    return 404; # managed by Certbot

}